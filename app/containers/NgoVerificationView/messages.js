/*
 * NgoVerificationView Messages
 *
 * This contains all the text for the NgoVerificationView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NgoVerificationView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NgoVerificationView container!',
  },
});
