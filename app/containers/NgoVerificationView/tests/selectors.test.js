// import { fromJS } from 'immutable';
// import { selectNgoVerificationViewDomain } from '../selectors';

describe('selectNgoVerificationViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
