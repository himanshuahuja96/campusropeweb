/*
 * NgoList Messages
 *
 * This contains all the text for the NgoList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NgoList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NgoList container!',
  },
});
