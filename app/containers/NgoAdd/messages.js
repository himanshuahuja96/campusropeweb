/*
 * NgoAdd Messages
 *
 * This contains all the text for the NgoAdd container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NgoAdd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NgoAdd container!',
  },
});
