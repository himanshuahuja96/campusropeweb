/*
 * NgoView Messages
 *
 * This contains all the text for the NgoView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NgoView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NgoView container!',
  },
});
