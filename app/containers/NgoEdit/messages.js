/*
 * NgoEdit Messages
 *
 * This contains all the text for the NgoEdit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NgoEdit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NgoEdit container!',
  },
});
